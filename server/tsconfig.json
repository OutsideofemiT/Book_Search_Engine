{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",       // Only compile files from server/src
    "outDir": "./dist"        // Output files to server/dist
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
